@model PostViewModel

<div class="posts">
    <div class="post">
        <h2 class="post-title">@Model.Title</h2>
        <p class="post-text">@Model.Text</p>
        <p class="post-source">
            <a href="@Model.SourceUrl">Ссылка на источник</a>
        </p>
    </div>
</div>